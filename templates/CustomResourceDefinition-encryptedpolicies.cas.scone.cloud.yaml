---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.9.0
  creationTimestamp: null
  name: encryptedpolicies.cas.scone.cloud
spec:
  group: cas.scone.cloud
  names:
    kind: EncryptedPolicy
    listKind: EncryptedPolicyList
    plural: encryptedpolicies
    shortNames:
    - epol
    singular: encryptedpolicy
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.state
      name: STATE
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: EncryptedPolicy is the Schema for the encryptedpolicies API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: EncryptedPolicySpec defines the desired state of EncryptedPolicy
            properties:
              casAddress:
                type: string
              encryptionKey:
                type: string
              policy:
                type: string
            required:
            - casAddress
            - encryptionKey
            - policy
            type: object
          status:
            description: SconePolicyStatus defines the observed state of SCONE policies (SignedPolicy and EncryptedPolicy).
            properties:
              hash:
                type: string
              revision:
                type: string
              state:
                type: string
            required:
            - hash
            - state
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}